package edith;

import javafx.application.Application;

/** Launches Edith's JavaFX application. */
public class Launcher {
    /** Starts Edith's graphical interface. */
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
